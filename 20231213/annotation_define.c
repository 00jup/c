#include <stdio.h>

int main()
{
    printf("1\n");
#if 0
    void test(){
        subddd
    }
#endif

    printf("2\n");

    // int ch;
    // while ((ch = getchar()) != EOF)
    //     putchar(ch);

    // return 0;

    char buf[100];
    gets(buf);
    puts(buf);
}